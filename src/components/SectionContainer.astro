---
const { class: className, id, title } = Astro.props
---

<section
  id={id}
  data-section={id}
  class={`section ${className} scroll-m-20 w-full mx-auto container`}
>
  <h1 class="text-center font-bold mb-10 mt-10 text-3xl lg:text-5xl">{title}</h1>
  <slot />
</section>